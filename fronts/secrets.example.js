// Copia este archivo como  secrets.js  (mismo folder) y pon tu llave de Gemini.
// secrets.js está en .gitignore: NO se sube al repo.
// El dashboard lo carga solo y genera los insights con IA sin preguntar.
window.GEMINI_KEY = "TU_GEMINI_API_KEY_AQUI";
